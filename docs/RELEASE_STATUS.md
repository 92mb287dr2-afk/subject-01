# Состояние подготовки релиза — 19 сентября 2026

**PRE-BIRTH / CANDIDATE. Официальное существо не создано.**

Это инженерная реализация значительной части договора. Полная готовность
релиза не заявляется: ниже отделены выполненные проверки от пробелов.
Номера C относятся к неизменённым требованиям [ACCEPTANCE.md](ACCEPTANCE.md).

## Что работает

- Один процесс владеет каталогом через блокировку ОС. SQLite с WAL и FULL sync
  атомарно сохраняет состояние, события и подтверждение команды. Идентификатор
  запроса предотвращает повторное применение. Интерфейс получает только подтверждённые тики.
- Независимые числовые модели управления и исследования получают одинаковый
  доступный опыт. У исследователя нет ссылки на мир, тело или файловое хранилище.
  Арбитр единолично выбирает реальный моторный вектор.
- У абстрактного планарного тела четыре сочленения. Их углы отображаются в мире.
  Это ограниченная механика с тягой, не биологическая модель человеческой ходьбы.
- Исследователь предлагает ограниченные изменения силы, сначала строит прогноз,
  затем выполняется постепенная проба. Расхождение вызывает компенсацию новыми
  действиями с затратами энергии/материала. Выбор одного из трёх темпов обучения
  меняется по ошибкам прогноза, без внешних меток задач.
- Консолидация выбирает числовые воспоминания по ошибке прогноза. У временного
  опыта ограниченное окно; выбранные воспоминания защищены контрольными суммами.
  Текущие модели могут уточняться, воспоминания от этого не стираются.
- Тяжёлое повреждение включает восстановление. Минимальные потоки энергии и
  материала явно записаны в законах и учтены в балансе. Сброс в начальное состояние отсутствует.
- Исключительное удаление указанного воспоминания или уничтожение ядра требует
  предварительного описания, точной фразы, одноразового разрешения на 60 секунд
  и совпадения хеша цели. После уничтожения ядра автоматического рождения замены нет.
- В UI три графа, память, гипотезы, состояние восстановления и полный журнал.
  Модельные передачи — реальные произведения входов и весов последнего прогноза.
  Все модельные прогнозы и обновления журналируются с версиями матриц.

## Фактически выполнено локально

`PYTHONPATH=src python -m pytest`: **35 passed, 2 skipped**.
Пропущены два браузерных сценария; отдельный CI выполняет их в Chromium.

В этих проверках есть:

- C01: 1000 последующих шагов после сериализации дают совпадающие хеши всех компонентов.
- C02: настоящий дочерний процесс завершается через `os._exit` на пяти границах
  транзакции; при восстановлении получено целое старое или целое новое состояние.
  Повтор команды после перезапуска не дублируется.
- C03: второй процесс отклонён, после остановки блокировка освобождается (локально Linux).
- C04–C09: пауза по внешним часам, независимость от UI, память после повреждения,
  пересмотр модели, подтверждения исключений и независимость от скрытых меток.
- C11–C13: ограниченные органы/параметры, общий опыт при независимых состояниях,
  один арбитр реальных действий.
- C15–C17: разделение модельной и физической пробы, намеренно неверный прогноз,
  прекращение пробы и компенсация без отката времени/воспоминаний.
- C19–C21: восстановление и баланс ресурсов, отсутствие неподтверждённых кадров
  при ошибке записи, соответствие графа числовым вычислениям, HTTP-защита.

Это тестовое покрытие указанных аспектов, а не автоматическое закрытие каждого
более широкого требования договора. Повреждение накопителя и потеря всех копий
не эквивалентны проверенному аварийному завершению процесса.

## Предварительно заданные опыты

Протокол: [experiments/protocol.json](../experiments/protocol.json), сохранён до
первого измерения. Повтор: `PYTHONPATH=src python experiments/evaluate.py`.

| Seed | Ошибка обученной модели / замороженной | Адаптивный метод / фиксированный |
| --- | ---: | ---: |
| 23 | 0.747119 | 0.792846 |
| 41 | 0.855319 | 0.637296 |
| 97 | 0.777661 | 0.604944 |
| Среднее | 0.793366 | 0.678362 |

Предварительные пороги среднего отношения: 0.85 и 0.90 соответственно; оба
пройдены. Для первого опыта обучение остановлено перед 200 новыми пробами тела.
Во втором предсказание оценивается **до** обновления на следующих 160 образцах;
это онлайн-адаптация, не замороженная контрольная выборка. Отдельный seed=41
не достиг бы порога 0.85, если бы он применялся по каждому seed; протокол задавал среднее.

Эти опыты поддерживают ограниченные свойства C10/C14/C18. Они ещё не доказывают
освоение карты тела и улучшение методов на независимо заданных новых задачах
полного организма. Снижение ошибки не является доказательством сознания.

## Измеренные бюджеты и пределы

Короткий локальный прогон 100 тиков: среднее около 20 мс, p95 около 29 мс при
периоде 50 мс. Накоплено около 4.9 КБ/тик: ориентировочно 8.4 ГБ/сутки при 20 Гц.
Это предварительное измерение в рабочей среде разработки, не на ПК владельца;
долгий прогон и рост памяти могут изменить цифры. До постоянной жизни требуются
нагрузочный прогон и решение о хранении/архивировании полного журнала.
При свободном месте менее 64 МиБ следующий тик не выполняется.

## Что ещё блокирует готовый релиз

1. Полный эксперимент C10/C14/C18: причинное освоение тела и оценка адаптации
   методов на новых задачах целого контроллера. Не подменять его успехом линейного
   предиктора в упрощённом протоколе.
2. Единый цикл проверки всех структурных изменений сети. Сейчас рост сенсорной
   сети использует прежнее локальное правило ошибки; новые модельные пробы
   применены только к силе тела. Структурная пластичность ещё не проходит весь
   путь внутренней проверки, probation и ресурсной компенсации.
3. Полнота вмешательств наблюдателя: реализовано чтение всего состояния и точечные
   разрушительные исключения, но нет полного редактирования всех защищённых
   представлений и правил ядра с предварительным просмотром последствий.
4. Длительные измерения CPU/RAM/диска, политика архивов и подтверждённая проверка
   на Windows и в браузере. CI добавлен, результаты должны быть проверены отдельно.
5. C22: окончательный отчёт, зафиксированные законы/бюджеты, процедура первого
   рождения и отдельное решение владельца после ознакомления с результатом.

Обычный запуск `start-subject.bat` запускает только кандидата и не обходит эти условия.
